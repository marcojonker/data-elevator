'use strict';

//Load the configuration by filename

var ConfigLoader(path) {
    
}

ConfigLoader.Load(path, environment) {
    return {
        "templateFile" : "",
        "type": "MONGODB",
        "connectionString" : ""
        
    }
}

module.exports = ConfigLoader;